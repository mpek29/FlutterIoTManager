import 'package:flutter/material.dart';

/// Application color constants
class AppColors {
  AppColors._();

  // Brand colors
  static const Color primaryDark = Color(0xFF272D39);
  static const Color backgroundLight = Color(0xFFF3F2F7);
  static const Color bottomBarBackground = Color(0xFFF7F7F7);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFFF3B2F);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF2196F3);

  // Mode colors
  static const Color ecoGreen = Colors.green;
  static const Color basicBlue = Colors.blue;
  static const Color dailyOrange = Colors.orange;

  // Text colors
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Colors.grey;
  static const Color textWhite = Colors.white;

  // State colors
  static const Color online = Colors.green;
  static const Color offline = Colors.grey;
}
